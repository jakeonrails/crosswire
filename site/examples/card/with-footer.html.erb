<%= cw.card variant: :outlined do |c| %>
  <% c.header { "Team plan" } %>
  <% c.body { "Up to 20 seats, shared workspaces, SSO." } %>
  <% c.footer { cw.button "Upgrade", variant: :primary } %>
<% end %>
